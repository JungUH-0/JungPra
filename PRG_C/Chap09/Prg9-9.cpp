/**************************************************************
 * 인덱스와 포인터 모두를 활용해서                            *
 * 배열의 요소에 접근하는 방법을 보여주는 프로그램            *
 **************************************************************/
#include <iostream>
using namespace std;

int main()
{
  // 배열 선언과 초기화
  int numbers[5] = {10, 11, 12,13, 14};
  // 인덱스를 사용해서 배열의 요소에 접근
  cout << "인덱스를 사용해서 배열의 요소에 접근하기" << endl;
  for(int i = 0; i < 5; i++)
  {
    cout << numbers[i] << "   ";
  }
  cout << endl;
  // 포인터를 사용해서 배열의 요소에 접근
  cout << "포인터를 사용해서 배열의 요소에 접근하기" << endl;
  for(int i = 0; i < 5; i++)
  {
    cout << *(numbers + i) << "   "; //여기있는 numbers 는 인트형 자료형이기에 포인터전체로 역어버리면 자료형의 사이즈만큼 움직임 주소값
  }
  return 0;      
}